# Setting up the base project

Let's clone and deploy the base application we'll be using for this workshop.

```
git clone -b less_secure https://github.com/EducloudHQ/health_ai_final.git
```

Once you've cloned the application, open it up in your favority IDE.

Run the command `npm i` to install dependencies inside the `package.json` file.
